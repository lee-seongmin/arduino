pc에 똑같은 라이브러리가 깔리지 않도록 주의해야한다.

low레벨에서도 가능하고 라이브러리를 이용해서도 가능하다.
byte단위
1byte단위로 통신이 가능하고 EEPROM dp

각자의 프로토콜이 있는데

로우 -> 하이 바이트 저장시킨다.

111=7인데
7*2^8으로 계산해야한다.


8bit 바이트 = lowByte(16bit 정수); // 16bit 정수 하위 바이트 추출

8bit 바이트 = highByte(16bit 정수); // 16bit 정수 상위 바이트 추출

EEPROM.write(#, 8bit 바이트); // EEPROM 바이트 단위 저장, #: address, 0~1023

8bit 바이트 = EEPROM.read(#); // EEPROM 바이트 단위 읽기

word(상위 바이트, 하위 바이트); // 2 바이트를 16bit 정수로 합침

공용체는 동일한 메모리 위치에 다른 데이터 유형을 저장할 수 있는 c에서 사용할 수
있는 특수한 데이터 유형입니다.
Union은 동일한 메모리 위치를 다 목적으로 사용하는 효율적인 방법을 제공합니다.

t = transmit 송신 (trigger)

R = Receive  수신 (echo)

물체에 평행하게 배치되어야한다.

갔다가 오는 시간이라 v=s/t/2로 해주면 속도가 나온다.
5v가 뜨고 있는 시간을 에코핀에서 내보낸다.
디지털하이가 뜨는 시간을 측정한다.
